server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;  # For client-side routing
    }

    error_page 404 /index.html;  # Handle 404 errors for SPA
}
